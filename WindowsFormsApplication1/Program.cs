using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Windows.Forms;

namespace WindowsFormsApplication1
{
    /// <summary>
    /// now dont' forget
    /// </summary>
    static class Program
    {
        /// <summary>
        /// The main entry point for the application.
        /// </summary>
        [STAThread]
        static void Main()
        {
            //Test bsyap86
            //Soon Hui is always right
            Application.EnableVisualStyles();
            Application.SetCompatibleTextRenderingDefault(false);
            Application.Run(new Form1()); //what is this code doing?
        }


        /// <summary>
        /// Now start to do a branch 1, SH also wants to work on it. i want to disturb you
        /// </summary>
        private static void Branch1()
        {
            //the content here is meant for b1
            // the b1 now more updates
            // sorry boss, let me work on branch 1 first
        }
    }
}
